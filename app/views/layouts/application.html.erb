<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div>
  <header class="p-3 text-bg-dark">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
          <image src="https://i.pinimg.com/originals/a6/25/cc/a625cc89bdcf3cbb0c7940e604db01b9.png" src="yourfallback.png" width="70" height="40"/>
        </a>

        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><a href="<%= home_path %>" class="nav-link px-2 text-secondary">Главная</a></li>
          <li><a href="<%= about_path %>" class="nav-link px-2 text-white">О нас</a></li>
        </ul>

        <div class="text-end">
          <% if Current.user %>
            <table style="border-collapse: separate;border-spacing: 10px 0px;">
              <tr>
                <td style="text-align: center; vertical-align: middle;">Logged in as: <%= Current.user.email %></td>
                <td style="text-align: center; vertical-align: middle;">Balance: <%= Current.user.balance %></td>
                <td style="text-align: center; vertical-align: middle;"><%= button_to 'ЛК', edit_balance_path, method: :get, class: "btn btn-outline-light me-2" %></td>
                <td style="text-align: center; vertical-align: middle;"><%= button_to 'Logout', logout_path, method: :delete, class: "btn btn-outline-light me-2" %></td>
              </tr>
            </table>
          <% else %>
            <table style="border-collapse: separate;border-spacing: 10px 0px;">
              <tr>
                <td style="text-align: center; vertical-align: middle;"><%= button_to 'Sign Up', sign_up_path, method: :get, class: "btn btn-warning" %></td>
                <td style="text-align: center; vertical-align: middle;"><%= button_to 'Login', sign_in_path, method: :get,  class: "btn btn-outline-light me-2" %></td>
              </tr>
            </table>
          <% end %>
        </div>
      </div>
    </div>
  </header>
<!--  <div class="container">-->
    <%= yield %>
<!--  </div>-->
  </div>
  </body>
</html>
